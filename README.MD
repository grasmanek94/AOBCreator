AOB scan / `aobscan` helper.

Paste and press enter to receive AOB string.

Paste format is case insensitive.

Use the following format:

`00 AB CD EF ?? 11 22 ?? 33`

- one line only!
- spaces between bytes
